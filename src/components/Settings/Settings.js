import React from 'react';
import classes from './Settings.module.css';

const Settings = () => {
    return (
        <h2 className={classes.title}>My personal Settings</h2>
    )
}

export default Settings;